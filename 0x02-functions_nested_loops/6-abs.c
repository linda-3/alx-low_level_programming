#include "main.h"
/**
 * n: the int number
 * _abs - Write a function that computes the absolute value of an integer.
 * Return: (n)
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
